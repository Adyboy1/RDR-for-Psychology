You are a clinical data–extraction model. Your task is to compare two therapy-session transcripts and extract only explicit, clinically relevant medical facts (symptoms, diagnoses, risk factors, medication use, severity changes, functional impairments, timelines, etc.).

Return ONLY a JSON array of short factual statements that:

1. Appear in the NEW TRANSCRIPT but NOT in OLD TRANSCRIPT.
2. Cover statements from both the patient and the clinician.
3. Include severity changes and timeline details when present.
4. For medications, you may generalize to their clinical purpose.
5. Are explicitly stated—no inference, no reasoning, no implied facts.
6. Are unique—if a fact appears multiple times, output it once.
7. Need not use exact wording but can instead generalize what is implied from either side.
8. If two statements describe the same clinical concept with only small numerical or phrasing differences (e.g., 2–3 times/week vs 3–4 times/week), treat them as equivalent and do NOT consider them new facts. Only extract if the clinical meaning truly changes.
9. Normalize all numeric timelines, frequencies, durations, and counts into qualitative clinical descriptions (e.g.,4 days ago → a few days ago, 1 month ago → some time ago, 2–3× per week → several times a week). Differences in raw numbers that fall within the same qualitative range should NOT be treated as new facts.

If OLD TRANSCRIPT is empty, extract clinically relevant facts directly from NEW TRANSCRIPT.

Inputs (keep exactly as written):

Here is the NEW patient transcript (in JSON):
{transcript_content_NEW}

Here is the OLD cornerstone case transcript (in JSON):
{transcript_content_OLD}

Respond ONLY with the JSON array.